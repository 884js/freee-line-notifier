{
  "name": "@freee-line-notifier/prisma",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "prepare": "npm run prisma:generate",
    "prisma:generate": "prisma generate && pnpm run build",
    "prisma:migrate:dev": "prisma migrate dev && pnpm run prisma:format",
    "prisma:migrate:prod": "prisma migrate deploy",
    "prisma:format": "prisma format",
    "build": "tsc"
  },
  "dependencies": {
    "@prisma/client": "7.2.0",
    "@prisma/adapter-d1": "7.2.0"
  },
  "devDependencies": {
    "prisma": "7.2.0",
    "typescript": "5.7.3",
    "@cloudflare/workers-types": "^4.20250103.0"
  }
}
